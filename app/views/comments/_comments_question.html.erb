<%= turbo_frame_tag 'modal' %>
<div class="my-4 border border-slate-300 rounded-lg" id="comments_question">
  <div class="bg-sky-100 flex justify-center items-center">
    <h2 class="text-center font-semibold text-xl p-2">Comentarios:</h2>
  </div>
  <div class="comments">
    <% comments.each do |comment| %>
      <p class="border-t-2 py-2 border-slate-200 text-sm items-center">
        <%= comment.content %>
      </p>  
    <% end %>
  </div>
</div>